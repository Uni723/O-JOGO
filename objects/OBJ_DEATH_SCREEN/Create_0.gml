// --- Variáveis de controle ---
alpha_screen = 0;         // Fade-in da tela
wave_offset = 0;          // Offset das ondas do mar
restart_delay = 0;        // Tempo até permitir reinício

// --- Cores e estilo ---
col_bg = make_color_rgb(20, 40, 70);     // Azul profundo
col_wave = make_color_rgb(40, 90, 140);  // Azul claro das ondas
col_text = c_white;

// --- Texto ---
death_msg = "Voce afundou nas profundezas...";
restart_msg = "Pressione ENTER para renascer";
